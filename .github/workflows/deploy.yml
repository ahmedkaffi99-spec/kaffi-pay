name: Deploy Edge Functions → Supabase

on:
  push:
    branches:
      - claude/fait-fix-DjJ6q

jobs:
  deploy-functions:
    name: Deploy Edge Functions → Supabase
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Install Supabase CLI
        run: npm install -g supabase@latest

      - name: Deploy all Edge Functions
        run: |
          FUNCTIONS=(
            sms-webhook
            submit-depot
            submit-retrait
            admin-bot
            admin-action-ordre
            admin-stats
            ordres-bloques
            support-client
            get-ordre
            health-check
          )
          for fn in "${FUNCTIONS[@]}"; do
            echo "Deploying $fn..."
            supabase functions deploy "$fn" \
              --project-ref pasotcpwvdtpidelrqic \
              --no-verify-jwt
          done
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
